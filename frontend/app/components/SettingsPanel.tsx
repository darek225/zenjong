"use client";
import type { LocalProfile } from "../../lib/progression";
export default function SettingsPanel({ profile, onChange, onClose }: { profile: LocalProfile; onChange: (profile: LocalProfile) => void; onClose: () => void }) {
  const options: [keyof LocalProfile["settings"], string, string][] = [
    ["reducedMotion", "Reduced motion", "Disable lifted/expanding visual effects."], ["highContrast", "High contrast", "Increase interface and tile separation."],
    ["largeTiles", "Large tile faces", "Scale the board tiles for readability."], ["colorSafe", "Color-safe selection", "Use a strong outline instead of color alone."], ["ambientEffects", "Ambient effects", "Show room particles and post-processing."],
  ];
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/75 p-4"><section role="dialog" aria-modal="true" aria-labelledby="settings-title" className="w-full max-w-lg rounded-3xl border border-amber-100/20 bg-[#101c18] p-6 text-white"><header className="flex justify-between"><div><p className="text-xs uppercase tracking-[.35em] text-amber-200/60">Accessibility</p><h2 id="settings-title" className="mt-2 font-serif text-4xl text-amber-50">Settings</h2></div><button className="rounded-lg border border-white/15 px-3 py-2" onClick={onClose}>✕</button></header><div className="mt-6 grid gap-3">{options.map(([id, label, description]) => <label key={id} className="flex cursor-pointer items-center justify-between gap-4 rounded-xl border border-white/10 p-4"><span><strong className="block text-amber-50">{label}</strong><small className="text-white/50">{description}</small></span><input type="checkbox" checked={profile.settings[id]} onChange={event => onChange({ ...profile, settings: { ...profile.settings, [id]: event.target.checked } })} className="h-5 w-5 accent-amber-200" /></label>)}</div><p className="mt-5 text-xs text-white/40">Keyboard: Escape pause · H hint · U undo · S shuffle · R restart.</p></section></div>;
}