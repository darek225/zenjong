"use client";
import { useState } from "react";
const steps = [
  ["Find a free tile", "A tile is free when nothing covers it and at least one side is open."], ["Match the faces", "Numbered tiles match identical faces. Flowers match flowers, and seasons match seasons."], ["Use your tools", "Undo rewinds a match. Hint reveals a legal pair. Shuffle preserves the deal but changes the positions."], ["Save your table", "Pause or leave through the Zenjong button. Your verified hand is saved on this device."],
];
export default function TutorialOverlay({ onDone }: { onDone: () => void }) { const [step, setStep] = useState(0); const last = step === steps.length - 1; return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/70 p-4"><section role="dialog" aria-modal="true" aria-labelledby="tutorial-title" className="w-full max-w-md rounded-3xl border border-amber-100/20 bg-[#101c18] p-7 text-white shadow-2xl"><p className="text-xs uppercase tracking-[.35em] text-amber-200/60">Lesson {step + 1} / {steps.length}</p><h2 id="tutorial-title" className="mt-4 font-serif text-4xl text-amber-50">{steps[step][0]}</h2><p className="mt-4 leading-7 text-white/65">{steps[step][1]}</p><div className="mt-7 flex justify-between gap-3"><button className="text-sm text-white/45" onClick={onDone}>Skip tutorial</button><button className="rounded-xl bg-amber-200 px-5 py-3 font-bold text-[#17251d]" onClick={() => last ? onDone() : setStep(value => value + 1)}>{last ? "Begin playing" : "Continue"}</button></div></section></div>; }